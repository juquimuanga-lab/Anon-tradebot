version: "3.9"

services:
  bot:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
    ports:
      - "8001:8001"
    volumes:
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data:
