from __future__ import annotations

# Economical Jito bid defaults: start at the low end of the auction and let the
# existing affordability gate cap the actual tip. These are intentionally
# conservative defaults; operators can override them through environment vars.
