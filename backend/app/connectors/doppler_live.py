"""Compatibility shim for the Robinhood Doppler live execution adapter.

The implementation lives in app.execution.doppler_live. Keep this module as
an import-compatible bridge for connector-side runtime patches.
"""
from app.execution.doppler_live import *
